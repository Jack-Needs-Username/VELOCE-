# Veloce Vehicle Data

The current static prototype keeps the canonical vehicle library in `/vehicleLibrary.js` so GitHub Pages can load it without a build step.

Future build-system version:

- move canonical vehicle records into this folder
- export JSON or ES modules
- keep `vehicleId` as the stable join key for Explore, My Garage, Auctions, Digest, onboarding, and image attribution
